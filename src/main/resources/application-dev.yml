spring:
  datasource:
    url: jdbc:h2:mem:video;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
  jpa:
    hibernate.ddl-auto: update
  h2.console.enabled: true
server.port: 8082
